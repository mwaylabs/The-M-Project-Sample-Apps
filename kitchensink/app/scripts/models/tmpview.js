/*global kitchensink*/

kitchensink.Models = kitchensink.Models || {};


/**).
 * Models hold the business data of an application respectively the application's state.
 * It's usually the part of an application that is persisted to storage or be in sync
 * with a server.
 *
 * For further information go to:
 * http://the-m-project.org/docs/absinthe/Model.html
 * http://backbonejs.org/#Model
 */

(function() {
    'use strict';

    kitchensink.Models.TmpviewModel = M.Model.extend({
        // Here you can specify the model properties
    });

})();
